package InfosysInternalProjectPrep.InterviewPraPro;

public class EgForSingleton {
    /*
    public static void main(String[] args) {
        // step1: create a private static instance..
        private static DatabaseConnection instance;

        // step2: cereta a constructor as private so no one can create a new objects...
        private DataBaseConnection() {
            System.out.println("Database connected..");

            // step3: create public static  method to get the single instance
        }

        public static DatabaseConnection getInstance() {
            if (instance == null) {
                instance = new DataBaseConnection();
            }
            return instance;
        }
    }

     */


}
